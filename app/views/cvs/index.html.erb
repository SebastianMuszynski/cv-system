<div class="center">
  <%= link_to "Create new CV", new_cv_path %>
</div>

<% @cvs.each do |cv| %>
  <div class="cv-box">
    <div class="cv-preview">CV PREVIEW</div>
    <div class="cv-details">
      <div class="header">Details</div>
      <table>
        <% if cv.job.present? %>
          <tr>
            <td>Created for:</td>
            <td><%= cv.job.title %></td>
          </tr>
        <% end %>
        <tr>
          <td>Created date:</td>
          <td><%= cv.created_at.strftime("%H:%m %d.%m.%Y") %></td>
        </tr>
        <tr>
          <td>Feedback status:</td>
          <td class="feedback-status accepted">
            <%= link_to 'accepted', feedback_preview_cvs_path %>
          </td>
        </tr>
      </table>
      <div class="buttons center">
        <%= link_to "Edit CV", edit_cv_path(cv), class: "big-btn" %>
        <%= link_to "Remove CV", cv_path(cv), data: { confirm: "Are you sure?" }, method: :delete, class: "big-btn" %>
      </div>
    </div>
  </div>
<% end %>